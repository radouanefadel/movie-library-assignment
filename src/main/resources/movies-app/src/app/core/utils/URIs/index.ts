import { URI } from './model';
import { _movieURIs } from "./movie";
import { _directorsURIs } from './director';
import { _typesURIs } from './type';

export const movieURIs: Array<URI> = _movieURIs;
export const directorsURIs: Array<URI> = _directorsURIs;
export const typesURIs: Array<URI> = _typesURIs;